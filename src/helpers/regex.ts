// product id regex for validation using zod
export const productIdRegExp: RegExp = /^P-[A-Z0-9]+-\d{4}$/;

// start date regex for validation using zod
export const startDateRegExp: RegExp = /^\d{4}-\d{2}-\d{2}$/;